#include <bits/stdc++.h>
using namespace std;

int main()
{
    pair<int, string> s1, s2, s3, s4, s5;

    cin >> s1.first >> s1.second;
    cin >> s2.first >> s2.second;
    cin >> s3.first >> s3.second;
    cin >> s4.first >> s4.second;
    cin >> s5.first >> s5.second;

    cout << s1.first << " " << s1.second << endl;
    cout << s2.first << " " << s2.second << endl;
    cout << s3.first << " " << s3.second << endl;
    cout << s4.first << " " << s4.second << endl;
    cout << s5.first << " " << s5.second << endl;

    return 0;
}
