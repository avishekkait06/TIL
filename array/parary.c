#include <stdio.h>




int main() {
    
    int n ;
    scanf("%d",&n);
    int a[n];
    for(int i=0 ; i < n ; i++){
        scanf( "%d", &a[i]);
    }
printf("%d",2[a]);//In C and C++, a[i] and i[a] print the exact same value.
    return 0;
}